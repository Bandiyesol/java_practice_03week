spring:
  application:
    name: practice_03week

  datasource:
    url: jdbc:mysql://localhost:3306/java_study?serverTimezone=Asia/Seoul
    username: root
    password: bandi0806aa**!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
